# PETLIBRO camera POC configuration.
#
# No PLAF203 source is configured intentionally. go2rtc v1.9.14 includes
# TUTK transport primitives but no confirmed PLAF203 AV protocol/source.
# Adding a guessed tutk:// source or dynamic cloud contract here would be
# misleading and could expose credentials. See docs/camera.md.

api:
  listen: ":1984"

streams: {}
